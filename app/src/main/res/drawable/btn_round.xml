<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/main" /> <!-- 버튼 내부 색상 -->
    <stroke
        android:width="1dp"
        android:color="@color/gray" />
    <corners android:radius="7dp" /> <!-- 모서리 둥글게 설정, 필요에 따라 조절 -->
</shape>